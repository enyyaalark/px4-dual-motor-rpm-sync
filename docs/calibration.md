# 校准流程

## 1. 测量工具

- 独立激光转速计或等效可信参考（具体型号 `TBD`）。
- 逻辑分析仪/示波器测量霍尔周期与 PWM 脉宽。
- 万用表确认电压、极性与共地。

## 2. 每转有效脉冲数

1. 拆桨并将电机可靠固定。
2. 同时记录参考 RPM、霍尔频率和至少 10 秒原始脉冲数据。
3. 在多个稳定转速点计算 `ppr = 60 * frequency / reference_rpm`。
4. 检查各点是否接近同一整数/稳定值；异常时检查安装、磁体和毛刺。
5. 将确认结果、证据和不确定度写入硬件配置，未经复核不启用闭环。

成员 A 负责固定、供电边界、转速计与逻辑分析仪的物理操作和原始数据采集；成员 B
负责用 `tools/calibrate_ppr.py` 计算候选值并对照误差。每台电机的输入至少包含两个
不同稳定点，原始 CSV 不得改写，派生结果写入 `data/processed/`。

当前 KWS-ZS2508 只有量程 `300–200000 RPM` 和分辨率 `1 RPM` 信息，精度仍为
`TBD`。分辨率不能代替精度；在取得可追溯规格前，工具必须报告
`UNVERIFIED_REFERENCE_ACCURACY`，不得据此声称满足 3% RPM 误差目标。

### 2026-09-10 PPR 多点标定结果

两台拆桨电机分别通过同一已验证捕获通道采集，每台选择三个稳定转速点；这证明两套
电机/磁体/霍尔安装各自的有效 PPR，不替代最终 `PA0/PA1` 双路同时旋转验证。现场
固件输出为历史 `rpm_sync_capture,v1`，精确烧录提交仍为 `TBD`；其 1 MHz TIM2 周期
字段用于本次离线计算。

| 电机 | 参考 RPM | 周期中位数（µs） | PPR 估计 | 候选 PPR | 观测误差 |
|---|---:|---:|---:|---:|---:|
| motor1 | 1735 | 34787.0 | 0.994111 | 1 | 0.588920% |
| motor1 | 2358 | 25536.0 | 0.996448 | 1 | 0.355214% |
| motor1 | 3100 | 19485.5 | 0.993294 | 1 | 0.670557% |
| motor2 | 1770 | 33475.5 | 1.012630 | 1 | 1.263028% |
| motor2 | 2320 | 25436.0 | 1.016751 | 1 | 1.675063% |
| motor2 | 2980 | 20293.0 | 0.992176 | 1 | 0.782397% |

两台电机的三个点均收敛到整数 `PPR = 1`，该值可进入集中配置。若把 KWS-ZS2508
读数作为本次原型的工作参考，两台电机最大观测误差分别为 0.670557% 和
1.675063%；由于其精度没有可追溯来源，正式工具结论仍为
`UNVERIFIED_REFERENCE_ACCURACY`，不能据此关闭不超过 3% 的精度验收项。

原始 UART 文件、现场文件名与逻辑电机/点位映射、标定输入和工具输出分别见
`data/raw/2026-09-10/`、`data/processed/2026-09-10-ppr-calibration-sources.csv`、
`data/processed/2026-09-10-ppr-calibration-input.csv` 和
`data/processed/2026-09-10-ppr-calibration-results.csv`。最高测试脉冲频率为
51.320212 Hz；1 Hz UART 快照不能独立证明无漏脉冲，TIM2 数字滤波和最高预期频率
结论仍需逻辑分析仪证据。

## 3. 电调 PWM

- 暂按 1000–2000µs 设计，但实际最小、启动、最大和失联行为必须按电调说明书与无桨测试确定。
- 校准时先验证逻辑分析仪波形，再连接 ESC 信号；保持可立即断电。
- 记录 3.3V 控制高电平是否可靠被接受。

## 4. RPM 过滤与超时

- 从原始周期数据选择合理的中值/低通策略。
- 超时阈值必须大于最低受控 RPM 的正常脉冲周期，同时足够快地归零。
- 人为断开霍尔信号，确认 RPM 归零、故障置位、积分冻结。

## 5. 控制参数

- 先固定 `Ki = 0`，从很小的 `Kp` 和修正限幅开始。
- 每个参数点使用相同电池状态、油门点和测试时长。
- 若加入 PI，记录积分限幅、抗饱和触发和恢复过程。

所有数值在实测前均为 `TBD`，不得从其他电机项目直接复制。
