# AI 协作指南

本文件供 Codex、Claude Code 和其他仓库级 AI 助手使用。目标是让新会话快速理解项目、从正确任务继续工作，并留下可追溯的交接信息。它描述长期规则，不保存容易过期的“完成百分比”。

## 开始前必读

按顺序读取：

1. `README.md`：范围、当前阶段和快速开始。
2. `docs/architecture.md`：系统边界、信号链和状态机。
3. `docs/open-questions.md`：不得猜测的硬件信息。
4. 与当前任务相关的 GitHub Issue、Milestone 和 Project 项目。
5. 涉及接线、供电、电机或台架时，额外完整读取 `docs/safety.md` 和 `docs/wiring.md`。

若文档、代码和 Issue 不一致，先指出冲突，不要静默选择其中一个。安全规则始终优先。

## 项目概要

- PX4 负责遥控输入、姿态和上层飞行控制。
- STM32G431 负责双路霍尔测速、有限同步修正、故障判断、旁路控制和 UART 遥测。
- HCT157 的 A 输入接 PX4 原始 PWM，B 输入接 STM32 修正 PWM；STM32 未就绪时必须默认 PX4 直通。
- 第一阶段只使用标准 PWM 和 UART，不实现 DShot、CAN/DroneCAN 或 PX4 串口解析模块。
- 第一阶段只验证两个基础指令相同时的同步，不覆盖 PX4 主动差动推力。
- 当前是三周台架实验原型，不是飞行认证系统，不得表述为已经具备飞行安全性。

关键目录：

```text
firmware/stm32/App/   C++17 应用层和硬件无关逻辑
firmware/stm32/       CubeMX/HAL 集成说明与硬件配置模板
docs/                 架构、电气、算法、安全、计划和风险
tools/                CSV 校验与绘图工具
tests/                主机侧测试
data/raw/             不可改写的原始实验日志
data/processed/       可重新生成的处理结果
```

## 进度的唯一事实来源

- 工作项、负责人、阻塞和状态：GitHub Issues 与 [Project 看板](https://github.com/users/enyyaalark/projects/1)。
- 截止日期和阶段验收：GitHub Milestones。
- 架构、安全边界和操作规范：版本控制中的 `docs/`。
- 已验证结果：关联 Issue 的测试记录、原始 CSV、测试条件和对应提交。
- `README.md` 只给出阶段摘要；本文件不维护逐项实时状态。

详细状态流转和评论模板见 `docs/progress-tracking.md`。不得另建互相独立的进度表或把计划目标写成已验证结果。

## 每次 AI 工作流程

### 开始任务

1. 检查当前分支、`git status` 和最近提交；保留所有不属于当前任务的用户改动。
2. 找到对应 Issue，阅读目标、前置任务、验收标准、安全注意和最近评论。
3. 搜索代码、文档、开放 Issue 和历史提交，确认没有已有实现或并行任务。
4. 核对依赖和 `TBD`。前置条件不满足时，在 Issue 记录阻塞原因和解除条件，不绕过安全步骤。
5. 一次只处理一个可独立验收的 Issue；使用短功能分支和 Pull Request，不创建 `develop` 分支。

### 实施期间

- 变更保持小而可验证；每个模块先单独验证，再接入完整状态机。
- 发现范围外问题时，记录或创建独立 Issue，不顺手扩张当前实现。
- 改变接口、算法、安全行为或测试方法时，同步更新相关文档。
- 实验每次只改变一个主要变量，保留原始数据，记录硬件配置、固件提交和测试条件。
- 若两名成员或两个 AI 会话可能修改同一区域，先在 Issue 留言说明文件范围和预期产出。

### 完成与交接

1. 执行适用的检查并保存结果；没有运行的检查要明确说明原因。
2. 对照 Issue 的每条验收标准，不能满足时不得标为 `Done`。
3. 在 Issue 留下：改了什么、提交/PR、测试证据、未验证项、下一步和建议状态。
4. 行为或文档基线变化时更新 `CHANGELOG.md`。
5. 仅当验收和证据齐全时关闭 Issue；需要他人复核或实机验证时进入 `Review` 或 `Test`。

AI 的最终交接必须包含：变更摘要、运行的检查及结果、证据位置、仍然未知或未验证的内容、对应 Issue/Project 状态。

## C++ 与 STM32 规则

- STM32 应用层使用 C++17，文件使用 `.cpp`/`.hpp`，代码放在 `rpm_sync` 命名空间。
- CubeMX 生成的 HAL/Core 可保持 C；通过薄适配层进入应用层，HAL 要求的回调按需使用 `extern "C"`。
- 中断和 HAL 回调只采集最少数据；RPM 计算、控制、故障判断和日志放到周期任务。
- 参数集中在 `firmware/stm32/App/app_config.hpp` 或未来明确的配置层，禁止散落魔法数。
- 最终 GPIO、定时器、DMA、时钟和板级接口尚未确认时只写 `TBD`；不得创建假装可用的 `.ioc`。
- 不在实时控制路径引入动态内存分配、阻塞 I/O 或无界循环。
- 闭环默认关闭。PPR、PWM 范围、增益、死区、超时和修正限幅必须经过标定或测试后才能启用。
- 必须保留低转速闭环禁止、脉冲超时、异常值检测、最终 PWM 限幅、修正限幅和故障停止修正。

## 不可妥协的安全边界

- 初期调试必须拆除螺旋桨。
- 带桨测试必须使用刚性固定台架、防护罩、独立急停和两人交叉复核；人员不得位于桨盘平面内。
- 电池和电调主电流不得经过面包板、洞洞板、杜邦线或信号线。
- PX4、STM32 和电调控制信号必须共地，但电压、极性和供电路径必须人工复核。
- 未确认的型号、引脚、电压、电平、协议和每转脉冲数一律写 `TBD`，不得猜测。
- 某侧霍尔或电机异常时不得继续积分或盲目增大另一侧输出。
- 不执行购买、付款、Release、飞行测试或仓库外公开发布，除非用户另行明确授权。

## 当前必须保持为 TBD 的信息

- PX4 飞控准确型号和固件版本。
- 接收机型号和协议。
- 电调型号、固件及 3.3V 控制电平兼容性。
- 电池节数和电压。
- 螺旋桨尺寸和旋向。
- 最终 STM32 定时器、DMA 和 GPIO 分配。
- 经独立转速计标定的每转有效脉冲数。

以 `docs/open-questions.md` 为完整清单；确认信息时同时更新该文档、相关配置和 Issue 证据。

## 基础检查

按改动范围运行：

```bash
c++ -std=c++17 -Wall -Wextra -Werror -fsyntax-only firmware/stm32/App/*.cpp
python3 -m unittest discover -s tests -v
python3 tools/plot_rpm_log.py data/demo/synthetic_rpm_log.csv --validate-only
git diff --check
```

主机语法检查不等于 STM32 交叉编译，synthetic/demo 数据不等于实机证据。不得声称未实际执行的测试已通过。

## 提交和数据规则

- 提交信息使用清晰前缀，例如 `docs:`、`feat:`、`fix:`、`test:`、`chore:`。
- 不提交账号、令牌、密钥、个人机器绝对路径、串口设备名、设备序列号或真实凭据。
- `data/raw/` 中的实验原始日志不可覆盖；处理产物写入 `data/processed/`。
- 示例数据必须明确标记为 `synthetic/demo`，禁止伪造真实实验数据或验证结论。

